import React from "react";

export const metadata = {
    title: "Bhaw Bhaw | Manage Pet Services & Products",
    description: "Monitor key metrics and gain insights into Bhaw Bhaw's operations. From sales performance to user activity, the dashboard equips admins with powerful tools for seamless management and analysis.",
    keywords: "admin home, platform management, Bhaw Bhaw tools, operational insights"
  };
  
  const Layout = ({ children }) => {
    
    return (
      <div>
        {children}
      </div>
    );
  };
  
  export default Layout;
    